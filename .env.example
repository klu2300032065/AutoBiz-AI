# Example Environment Variables
# If you decide to use a paid API later (like Tavily or SerpAPI), you can add it here.
# Currently, the application uses duckduckgo-search which does not require an API key.
SEARCH_API_KEY=your_optional_search_api_key_here
